# Cassette Interface

The PCB File is being made by importing gerber files first into KiCad, and mapping the schematics to it by applying footprints to the symbols, placing over the gerber geometry, and then removing the import gerber geometry

# To Do
- The diode pins are not lined up with what i expect to see from the traces provided with the gerber
- the capacitor still needs a footprint created at the custom pitch of 8.89mm
- all other placed footprints need to have a custom copy made that reflects the gerbers
